const expect = require('chai').expect;
const scrapper = require('../crawler/scrapper');
var should = require("should");


describe('API test', () => {
    it('GET event scrapping', function(done) {
        // Test Get All Events with token
        console.log(scrapper);
        done();
    });
});
